# Secrets — never commit
.env
*.env.local
Anthropic API.txt
STT TTS API.txt
* API.txt
*API*
*api*key*
*API*KEY*
*.key
*_secret
*_secret.*
*.pem

# Dev-session archives — local reference only, not product
B0 projects research.txt
Claude Code Clicky Chat.txt
Claude Code Clicky BEFORE Compact.txt
PLAN.txt
STARTER_PROMPT.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/
venv/
.venv/
env/

# Debug / scratch files
debug_*.jpg
debug_*.png
scratch_*
*.log

# Whisper model cache (downloaded on first run, large)
whisper-cache/
.cache/

# OS
Thumbs.db
.DS_Store
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local plugin settings (Superpowers install is local scope, gitignored by default)
.claude/settings.local.json

# Superpowers brainstorming visual-companion session files (mockups + events + server state)
.superpowers/
